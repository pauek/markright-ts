{
  "name": "markright",
  "version": "0.0.1",
  "type": "module",
  "files": [
    "internal"
  ],
  "main": "./internal/index.js",
  "module": "./internal/index.js",
  "types": "./internal/index.d.ts",
  "scripts": {
    "clean": "rimraf ./intermediate ./internal",
    "precompile": "npm run clean",
    "compile": "tsc -p src",
    "prebuild": "npm run compile",
    "build": "rollup -c",
    "pretest": "tsc -p test",
    "test": "node ./test/test.js"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "chalk": "^5.0.1",
    "colors.ts": "^1.0.20",
    "rimraf": "^3.0.2",
    "rollup-plugin-dts": "^4.2.2",
    "tslib": "^2.4.0",
    "typescript": "^4.6.4",
    "rollup": "^2.75.6"
  }
}
